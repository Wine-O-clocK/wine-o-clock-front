import { atom } from "recoil";

export const nowBubbleState = atom({
  key: "nowBubbleState",
  default: 0,
});

export const answerHelloState = atom({
  key: "answerHelloState",
  default: 0,
});

export const answerFirstState = atom({
  key: "answerFirstState",
  default: 0,
});

export const answerSecondState = atom({
  key: "answerSecondState",
  default: 0,
});

export const answerThirdState = atom({
  key: "answerThirdState",
  default: 0,
});

export const answerFourthState = atom({
  key: "answerFourthState",
  default: 0,
});
